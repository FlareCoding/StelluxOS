/*
 * Placeholder trust anchors. Replace with generated output from
 * gen_trust_anchors.sh for real certificate validation.
 */
#include <bearssl.h>
#include <stddef.h>

const br_x509_trust_anchor TAs[1] = { 0 };
const size_t TAs_NUM = 0;
